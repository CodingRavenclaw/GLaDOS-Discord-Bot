<!DOCTYPE module PUBLIC
        "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
        "https://checkstyle.org/dtds/configuration_1_3.dtd">

<module name="Checker">
    <!-- Tabs verbieten -->
    <module name="FileTabCharacter" />

    <!-- Zeilenlänge: MUSS direkt unter Checker hängen, NICHT unter TreeWalker -->
    <module name="LineLength">
        <property name="max" value="120"/>
    </module>

    <!-- Alles, was Syntax/Struktur des Codes betrifft, kommt unter TreeWalker -->
    <module name="TreeWalker">

        <!-- Javadoc für alle Methoden -->
        <module name="MissingJavadocMethod">
            <property name="allowMissingPropertyJavadoc" value="true"/>
        </module>

        <!-- Leerzeichen-Regeln -->
        <module name="WhitespaceAround" />
        <module name="WhitespaceAfter" />

        <!-- Immer Klammern um if/for/while -->
        <module name="NeedBraces" />

        <!-- Leere Blöcke finden -->
        <module name="EmptyBlock" />

        <!-- Import-Regeln -->
        <module name="AvoidStarImport" />
        <module name="UnusedImports" />

        <!-- Namens-/Strukturregeln -->
        <module name="LocalVariableName" />
        <module name="OuterTypeFilename" />
    </module>
</module>
